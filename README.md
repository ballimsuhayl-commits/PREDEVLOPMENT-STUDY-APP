# Urban Forge Pro

Single-page (static) build for personal use.

- Open `index.html` in a browser.
- Set your Gemini API key via **API Key**.
- Use **Scan**, adjust the draggable pin, run AI analyses, and export PDF/GIS.

Deploy on GitHub Pages by enabling Pages for the `main` branch (root) in repo settings.
